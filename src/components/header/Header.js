import { Button, Navbar } from "react-bootstrap"

export default () => {
    return (
        <header>
            <Navbar bg='dark'>
                <Button type='dark'>Hello</Button>
            </Navbar>
        </header>
    )
}